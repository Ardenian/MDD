import { Component } from '@angular/core';

/**
 * Placeholder route target until `features/trackers/SPEC.md` is implemented — keeps
 * `core/`'s routing skeleton real (buildable, navigable) rather than pointing at
 * modules that don't exist yet.
 */
@Component({
  selector: 'app-trackers-page',
  template: `<h1>Trackers</h1>
    <p>Not yet implemented — see <code>src/app/features/trackers/SPEC.md</code>.</p>`,
})
export class TrackersPage {}
