import { Component } from '@angular/core';

/**
 * Placeholder route target until `features/correlation/SPEC.md` is implemented — keeps
 * `core/`'s routing skeleton real (buildable, navigable) rather than pointing at
 * modules that don't exist yet.
 */
@Component({
  selector: 'app-correlation-page',
  template: `<h1>Correlation</h1>
    <p>Not yet implemented — see <code>src/app/features/correlation/SPEC.md</code>.</p>`,
})
export class CorrelationPage {}
