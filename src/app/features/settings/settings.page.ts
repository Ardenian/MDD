import { Component } from '@angular/core';

/**
 * Placeholder route target until `features/settings/SPEC.md` is implemented — keeps
 * `core/`'s routing skeleton real (buildable, navigable) rather than pointing at
 * modules that don't exist yet.
 */
@Component({
  selector: 'app-settings-page',
  template: `<h1>Settings</h1>
    <p>Not yet implemented — see <code>src/app/features/settings/SPEC.md</code>.</p>`,
})
export class SettingsPage {}
