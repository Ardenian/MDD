import { Component } from '@angular/core';

/**
 * Placeholder route target until `features/calendar/SPEC.md` is implemented — keeps
 * `core/`'s routing skeleton real (buildable, navigable) rather than pointing at
 * modules that don't exist yet.
 */
@Component({
  selector: 'app-calendar-page',
  template: `<h1>Calendar</h1>
    <p>Not yet implemented — see <code>src/app/features/calendar/SPEC.md</code>.</p>`,
})
export class CalendarPage {}
