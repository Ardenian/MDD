{
  "name": "@ard/api-spec",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TypeSpec source of truth for the Diary Calendar API contract (ADR 0004).",
  "scripts": {
    "build": "tsp compile .",
    "watch": "tsp compile . --watch",
    "format": "tsp format \"**/*.tsp\""
  },
  "devDependencies": {
    "@typespec/compiler": "^1.15.0",
    "@typespec/http": "^1.15.0",
    "@typespec/openapi3": "^1.15.0",
    "@typespec/rest": "^0.85.0"
  }
}
